# Rummy 101 — 8 Player Multiplayer

Mobile-first Rummy 101 game project with premium dark/teal UI.

## Run
npm install
npm start

## Render
Build command: `npm install`
Start command: `npm start`

The server serves `index.html` from the project root so the files can be uploaded easily from a phone to GitHub.
